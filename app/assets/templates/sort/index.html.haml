%h3.center Yo, here you gonna see visualized data of the merge sort, enjoy.
%br
%br

%form.form-horizontal
  .form-group
    %label.col-sm-4.control-label( for='inputArray' ) Array to sort
    .col-sm-4
      %input#inputArray.form-control( type='text' placeholder='1 2 3...' ng-model='iarray' )
  .form-group
    .col-sm-4
    .col-sm-4
      .btn.btn-success( ng-click='mergesort()' ng-if='iarray' ) Sort!

%div( ng-if='data' )

  %h4 Final result is: {{ data.result }}
  %br

  %table.table.table-bordered
    %tr
      %td Part A
      %td Part B
      %td Array
      %td Offset A
      %td Offset B
    %tr
      %td {{ data[counter].partA }}
      %td {{ data[counter].partB }}
      %td {{ data[counter].array }}
      %td {{ data[counter].offsetA }}
      %td {{ data[counter].offsetB }}

  .btn.btn-primary( ng-click='next()' ng-if='data[counter+1]' ) Step Up
  .btn.btn-primary( ng-click='prev()' ng-if='data[counter-1]' ) Step Down
  .btn.btn-success( ng-click='play()' ) Play
  .btn.btn-danger( ng-click='reset()' ng-if='counter > 0' ) Reset

